// Party model removed — party accounts are now represented by `User` documents
// with `role: 'party'` and `partyCode` set. This file remains as a placeholder
// to avoid accidental imports; prefer using `User` model instead.

export default null;
